#pragma once
#include <vector>
#include <iostream>
#include <cstdlib>
using namespace std;

class Validator
{
public:
	float validate(vector < vector <float> > data,
		vector <int> current_features);

	
};

